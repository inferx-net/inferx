apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: inferx-dashboard-ingress
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  rules:
    - host: dashboard.local
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: inferx-dashboard
                port:
                  number: 1250
